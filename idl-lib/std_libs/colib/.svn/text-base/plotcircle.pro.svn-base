pro plotcircle, x0, y0, d, _extra = _extra

; overplots a circle at a given location, of a given diameter

t = findgen(1000)/1000.*2*!pi

x = x0 + d/2.*cos(t)
y = y0 + d/2.*sin(t)

oplot, x, y, _extra=_extra

end