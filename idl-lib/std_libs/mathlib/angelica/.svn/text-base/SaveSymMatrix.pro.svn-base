pro SaveSymMatrix, M, fname
;	! Saves the lower  triangle of the giant and
;        ! symmetric matrix M in an unformatted file.

	print *, 'Brutally saving ', fname
        write(2)  M
        close(2)
	print *, 'File size should be ', n*(8*n+8)/2, ' bytes.'
        return
end
